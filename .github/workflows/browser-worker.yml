name: DOM browser worker

on:
  push:
    paths:
      - browser_worker/**
      - tests/browser_worker/**
      - pyproject.toml
      - .github/workflows/browser-worker.yml
  pull_request:
    paths:
      - browser_worker/**
      - tests/browser_worker/**
      - pyproject.toml
      - .github/workflows/browser-worker.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: pyproject.toml
      - run: python -m pip install -e ".[test]"
      - run: python -m playwright install --with-deps chromium
      - run: python -m pytest -c tests/browser_worker/pytest.ini tests/browser_worker -q
      - run: python -m ruff check --config browser_worker/ruff.toml browser_worker tests/browser_worker
      - run: python -m ruff format --check --config browser_worker/ruff.toml browser_worker tests/browser_worker
